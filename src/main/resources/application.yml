spring:
  profiles:
    include: env
  jpa:
    hibernate:
      ddl-auto: update
    
server:
  servlet:
    session:
      timeout: 15m
      cookie:
        http-only: true
        # secure: true
        domain: localhost:3000
        same-site: lax